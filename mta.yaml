_schema-version: "3.2"
ID: travustnstravelproc
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: travustnstravelproc-destination-content
  type: com.sap.application.content
  requires:
  - name: travustnstravelproc-destination-service
    parameters:
      content-target: true
  - name: travustnstravelproc-repo-host
    parameters:
      service-key:
        name: travustnstravelproc-repo-host-key
  - name: travustnstravelproc-uaa
    parameters:
      service-key:
        name: travustnstravelproc-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: travustnstravelproc_html_repo_host
          ServiceInstanceName: travustnstravelproc-html5-srv
          ServiceKeyName: travustnstravelproc-repo-host-key
          sap.cloud.service: travustnstravelproc
        - Authentication: OAuth2UserTokenExchange
          Name: travustnstravelproc_uaa
          ServiceInstanceName: travustnstravelproc-xsuaa-srv
          ServiceKeyName: travustnstravelproc-uaa-key
          sap.cloud.service: travustnstravelproc
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: travustnstravelproc-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: travustnstravelproc-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - travustnstravelproc.zip
      name: travustnstravelproc
      target-path: resources/
- name: travustnstravelproc
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: travustnstravelproc-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: travustnstravelproc-destination-service
    service-plan: lite
- name: travustnstravelproc-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: travustnstravelproc-xsuaa-srv
    service-plan: application
- name: travustnstravelproc-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: travustnstravelproc-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
